"""The admin dashboard: `server` (HTTP server + controller) and `contract`
(the schema/API versions the static dashboard/ front-end is reviewed
against). `python -m app.dashboard` starts the server."""
