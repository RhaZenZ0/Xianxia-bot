[pytest]
testpaths = tests/python
python_files = test_*.py
addopts = -ra
markers =
    unit: fast Python-only unit tests for presentation, content helpers, and pure logic
    integration: Python integration tests for repository/orchestration features still owned by Python
    contract: Python boundary tests for Go engine HTTP/RPC, startup, deployment, and release contracts
